<% if(version == 3) { -%>
import { defineComponent } from 'vue';

export default defineComponent({
  name: '<%= name %>',
  setup() {
    // component setup
  },
});
<% } else { -%>
<% if(lang === 'ts') { -%>
import Vue from 'vue';

export default Vue.extend({
  name: '<%= name %>',

  props: [],

  data() {
    return {};
  },

  computed: {},

  methods: {},
});
<% } else { -%>
export default {
  name: '<%= name %>',

  props: [],

  data() {
    return {};
  },

  computed: {},

  methods: {},
};  
<% } -%>
<% } -%>
