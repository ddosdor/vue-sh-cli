<% if(version == 3) { -%>
import { defineComponent } from 'vue';

export default defineComponent({
  name: '<%= name %>',
  setup() {
    // component setup
  },
});
<% } else { -%>
export default {
  name: '<%= name %>',

  props: [],

  data() {
    return {};
  },

  computed: {},

  methods: {},
};
<% } -%>
