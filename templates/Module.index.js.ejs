// Vuex <%= name %> module main file
import actions from './actions';
import mutations from './mutations';

const INITIAL_STATE = {
  // initial state
};

const getters = {
  // getters
};

// VUEX MODULE /////////////////////////////////////////////////////////////////
const Module = {
  state: INITIAL_STATE,
  namespaced: true,
  mutations,
  actions,
  getters,
};

export default Module;
